# Ticket Triager — managed-agent template

name: ticket-triager
model: claude-sonnet-4-6

system:
  file: agents/ticket-triager.md

tools:
  - type: agent_toolset_20260401
    default_config: { enabled: true }

skills:
  - { from_plugin: . }

callable_agents: []
