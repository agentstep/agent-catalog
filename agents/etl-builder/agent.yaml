# ETL Pipeline Builder — managed-agent template

name: etl-builder
model: claude-sonnet-4-6

system:
  file: agents/etl-builder.md

tools:
  - type: agent_toolset_20260401
    default_config: { enabled: true }

skills:
  - { from_plugin: . }

callable_agents: []
